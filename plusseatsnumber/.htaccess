Options -Indexes

# api/ 以外はすべて index.html へ
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_URI} !^/api/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^ index.html [L]
</IfModule>

# config.php への直接アクセスを禁止
<Files "config.php">
  Require all denied
</Files>
